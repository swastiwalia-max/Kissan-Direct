name: Deploy Frontend to Pages

on:
  push:
    branches: ["main"] # Change this to "master" if your primary branch is named master

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './frontend' # Tells GitHub to deploy directly from your folder

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
